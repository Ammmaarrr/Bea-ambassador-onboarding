import { FullArtboardPage } from "./FullArtboardPage";

/** Account step — coded form on all viewports (AccountMobileForm inside OnboardingMobilePage). */
export function AccountPage() {
  return <FullArtboardPage pageKey="account" />;
}
