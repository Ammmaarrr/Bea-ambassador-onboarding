{
  "name": "bea-ambassador",
  "private": true,
  "description": "Bea ambassador onboarding — Logic Yard",
  "scripts": {
    "postinstall": "npm run install:web",
    "dev": "npm run dev --prefix apps/web",
    "dev:clean": "npm run dev:clean --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "start": "npm run start --prefix apps/web",
    "lint": "npm run lint --prefix apps/web",
    "clean": "npm run clean --prefix apps/web",
    "install:web": "npm install --prefix apps/web",
    "install:tooling": "npm install --prefix tooling"
  }
}
